<h2>New Task</h2>

<%= render "form.html", Map.put(assigns, :action, task_path(@conn, :create)) %>

<div class="topnav">
  <span><%= link "Back to All Tasks", to: task_path(@conn, :index) %></span>
</div>
